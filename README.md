# File-List-Generator

 Get a folder and output a text file with a list of files located in that directory.

## Build info

 ```bash
 pyinstaller -F -n "File List Generator" --icon=.\file_list_generator\icon\list.ico .\file_list_generator\__main__.py
 ```

If there are issues with the one-file version, remove -F
